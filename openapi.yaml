openapi: 3.0.1
info:
  title: defaultTitle
  description: defaultDescription
  version: '0.1'
servers:
  - url: https://whipped-api.phloxcorp.io:448
paths:
  /search:
    get:
      description: Auto generated using Swagger Inspector
      parameters:
        - name: q
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Auto generated using Swagger Inspector
          content:
            application/json; charset=utf-8:
              schema:
                $ref: '#/components/schemas/searchResponse'
components:
  schemas:
    searchResponse:
      type: object
      properties:
        totalCount:
          type: integer
        items:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              itemName:
                type: string
              brandName:
                type: string
              storeName:
                type: string
              itemLink:
                type: string
              price:
                type: integer
              currency:
                type: string
